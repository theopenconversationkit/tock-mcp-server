apiVersion: v1
kind: Service
metadata:
  name: {{ include "tock-mcp-server.fullname" . }}
  labels:
    {{- include "tock-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "tock-mcp-server.selectorLabels" . | nindent 4 }}

